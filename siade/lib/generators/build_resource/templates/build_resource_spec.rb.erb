RSpec.describe <%= name %>::BuildResource, type: :build_resource do
  subject { described_class.call(response: response) }

  let(:response) { instance_double('Net::HTTPOK', body: body) }

  let(:body) do
    <%= name %>::MakeRequest.call<% if id_name %>(params: params)<% end %>.response.body
  end<% if id_name %>

  let(:params) do
    {
      <%= id_name %>: <%= id_name %>
    }
  end

  let(:<%= id_name %>) { valid_<%= id_name %>(:<%= provider_namespace.underscore %>) }<% end %>
end
