class <%= name %>::ValidateResponse < ValidateResponse
  # Pick one (mandatory for the emission guard):
  # declares_no_specific_errors!
  # raises SomeProviderError, kind: :some_kind

  def call
    #resource_not_found! if not_found?

    #return if valid?

    #unknown_provider_response!
    fail NotImplementedError
  end
end
